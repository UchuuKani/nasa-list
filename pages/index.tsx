import type { NextPage } from "next";

// components
import ListContainer from "../src/components/ListContainer";

const App: NextPage = () => {
  return (
    <div>
      <ListContainer />
    </div>
  );
};

export default App;
